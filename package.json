{
  "name": "relevance-parser-js",
  "version": "1.0.0",
  "description": "A parser for the BigFix Relevance Language.",
  "main": "lib/lexer.js",
  "scripts": {
    "test": "tape test/*.js",
    "coverage": "istanbul cover tape test/*.js",
    "travis": "istanbul cover tape test/*.js && cat ./coverage/lcov.info | coveralls"
  },
  "author": "BigFix",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/bigfix/relevance-parser-js.git"
  },
  "devDependencies": {
    "coveralls": "^2.11.4",
    "istanbul": "^0.3.18",
    "tape": "^4.2.0"
  }
}
